/**
 * Some top level config to make it easier to change
 * things
 */

module.exports = {
  // Whether to get results in browser
  clientResults: true,
  // Environment, specifically for getting results
  environment: 'test'
};
